# 2026-09-12

- Q2 光伏环形热力图（问题二_光伏与负载热力图.py）：应用户要求把黑夜/0 kW 从深蓝改为橙色系——colormap 改为 浅橙#ffe3c2 → 深橙#e65100（orange_pv），set_bad 同步浅橙，图例改为「黑夜 / 无光照（0 kW，浅橙）」。已通过 py_compile 语法检查；按用户硬性要求未在本机运行脚本（本机无 numpy/matplotlib），由用户在目标机自行执行重出图。
- 追加修改（同日第二轮）：应用户要求把色带改为完整色相渐变——起点橙 #fb8c00（黑夜/0 kW 端）→ 黄→黄绿→绿→青→蓝→紫 #7b1fa2（强光顶端），cmap 名 hue_orange2purple，set_bad 同步橙端，图例改「黑夜 / 无光照（0 kW，橙端）」。已过 py_compile，未本机运行。
- 第三轮：色带方向反转——黑夜/0 kW=紫端 #7b1fa2，强光/高值=橙端 #fb8c00（橙→紫从高到低）；图2 负载格子图弃用灰→红色带，改为与图1共用同一条橙→紫色相渐变（cmap2 = cmap），set_bad 同步紫端，图例/打印文案同步。已过 py_compile，未本机运行。
- 第四轮：色标分度值改为全年白天 P99（原为冬季白天 P90）——只有夏季左下角强光时段饱和到橙端，其余季节落在渐变中段，突出夏季光照最强；打印文案同步。已过 py_compile，未本机运行。
- 第五轮（仅图1环形图）：① savefig 后立即 plt.show(block=False)+pause，保存与弹窗同步；② 去掉 00:00/24:00 时间标签（仅留06/12/18）；③ 月份数字 fontsize 12→40 加粗；④ ax.set_xticks([]) 去掉度数刻度；⑤ 时间标签改为手动 text 并按角度 105/80/55 错开；⑥ norm 改 PowerNorm(gamma=1.6) 拉伸高功率区分度。图2：改回独立色带实例（同渐变、不共享对象），标尺仍用负载 LVMAX，不受图1影响。已过 py_compile，未本机运行。
- 第六轮（图1）：① 月份数字 40→22；② 删除环中心「内 → 外：00:00 → 24:00」文字（YEAR 保留并居中 0.50）；③ 图1 色标低端由紫色改为深灰渐变 #262626→#757575→过渡蓝→…→橙（cmap 改名 hue_orange2gray，set_bad #262626），图例色块改深灰、文案改「0 kW，深灰」。图2 保持独立紫色渐变不变。已过 py_compile，未本机运行。
- 第七轮（图1）：① 春夏秋冬标签外移至 outer+0.80、月份贴外缘 outer+0.14、ylim 扩至 +1.10，解决拥挤；② colorbar.set_ticks([]) 删除出力刻度数字；③ 零值端由深灰改浅灰 #f2f2f2 渐变（昼夜分界清晰），图例色块同步浅灰；④ 色带改非线性分段（0-15%浅灰、15-40%灰蓝→蓝→青→绿收窄、40-62%黄绿加密中低值、62-100%黄→橙→深橙拉开），norm 回线性 Normalize，删除 PowerNorm 导入。图2 未动。已过 py_compile，未本机运行。
- 第八轮：零值端灰色加深一档 #f2f2f2→#bdbdbd（8% 处 #a8a8a8，15% 过渡 #8fa0c4），set_bad/图例同步 #bdbdbd、文案改中灰。已过 py_compile，未本机运行。
- 第九轮：零值灰再加深 #bdbdbd→#9e9e9e（8% #8a8a8a、15% #7f92bd），set_bad/图例同步；月份数字去掉白色底衬 bbox，径向外移 outer+0.14→0.20。已过 py_compile，未本机运行。
- 第十轮：ax.set_yticks([]) 清除径向默认刻度数字（0.5/1/1.5 等）。已过 py_compile，未本机运行。
