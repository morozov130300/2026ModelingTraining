# AGENTS.md

## 仓库概况

这是一个以数学建模训练题目和原始数据附件为主的数据型仓库，目前没有发现源代码、构建配置、测试配置、README、Cursor 规则或其他代理说明文件。

核心材料位于 [数学建模训练题目.docx](题目/数学建模训练题目.docx)，主题是“基于血液检测数据的流感 A 辅助筛查与判别”。题目要求围绕两类人群建立可解释的分类/判别分析：

- 流感 A 患者组：`题目/附件/FLUA.xls`
- 健康对照组：`题目/附件/Healthy control.xlsx`

题目中的目标标签应由文件来源生成；“序号”是样本标识，不应作为预测特征。主要候选变量包括性别、年龄，以及 WBC、N、L、M、RBC、HB、PLT、RDW 等血液检测指标。

## 数据与分析约定

- 处理数据前先分别检查两个文件的表头、样本量、字段类型、缺失值、重复记录和异常/极端值；两份文件的年龄格式及其他变量格式可能不完全一致。
- 合并数据时保留来源类别字段，并统一字段名称、数值单位和分类变量编码。不要把样本编号泄漏到模型特征中。
- 由于类别数量可能不均衡，评估模型时不要只报告准确率；同时考虑混淆矩阵、敏感度/召回率、特异度、精确率、F1、ROC-AUC 或其他与筛查场景匹配的指标。
- 训练、验证和特征选择必须避免数据泄漏；异常值处理、标准化、特征筛选等步骤应只使用训练数据拟合，再应用到验证/测试数据。
- 结论仅用于数学建模意义上的辅助筛查研究，不应表述为临床诊断结论。报告推广性限制时应明确讨论样本数量、类别比例、样本来源、年龄和性别结构等因素。
- 问题 3 要求提供简洁、直观、便于执行的判别方案，并与较完整模型比较；简化方案应说明指标减少后的性能、使用成本和误判风险。

## 常用命令与验证

当前仓库没有可执行的构建、代码检查或测试入口，也没有 `package.json`、`pyproject.toml`、`Makefile`、`Cargo.toml` 等项目配置。Python 分析将在其他环境运行；本地环境应尽量保持零额外 Python 依赖，不要为了读取或处理数据在本地安装 Python 库。需要本地检查时，优先使用现有系统工具或仅进行文件级检查；不要假定存在 `build` 或 `test` 命令。

可用的只读检查示例：

```bash
# 查看仓库中的文件
find . -maxdepth 3 -type f -print | sort

# 查看 Git 工作区状态
git status --short

# 读取 DOCX 正文（环境提供 pandoc 时）
pandoc -t markdown '题目/数学建模训练题目.docx'
```

对数据分析代码的最低验证建议：

1. 先运行语法检查或导入检查，确认脚本可执行。
2. 对两个原始数据文件分别打印/核对工作表、行数、列名和关键字段类型。
3. 检查清洗前后样本数、缺失值统计、类别分布和异常值处理结果。
4. 固定随机种子，并输出独立验证或交叉验证结果；确认最终报告中的表格和图形来自可复现的运行结果。

注意：当前环境中已观察到 `FLUA.xls` 是传统二进制 Excel 文件，读取它需要支持 `.xls` 的工具；不要仅按 `.xlsx` 的 ZIP 格式处理它。`Healthy control.xlsx` 当前包含工作表 `HC`，表头对应题目列出的 11 个字段。

## 目录职责

- `题目/`：题目说明和原始附件，不应在原文件上直接覆盖式清洗。
- `题目/附件/`：输入数据文件。分析产物、清洗后的数据、图表、模型和报告应放在新增的明确目录中，并保留从原始数据到结果的处理链路。
- 根目录：代理说明及后续必要的分析入口；如果新增脚本，优先使用能表达用途的名称，并在脚本中明确输入文件路径和输出位置。

## 强制执行规则

- **禁止删除用户文档**：在执行任何任务时，严禁删除属于用户的任何文件，包括但不限于：计划方案文档（如 `数据预处理方案.md`）、题目文件（`题目/` 目录下的所有内容）、数据预处理脚本（如 `数据预处理.py`）、数据预处理结果（`数据预处理结果/` 目录下的所有内容）、分析报告、分析脚本及产出物。所有分析任务的数据来源必须是预处理后的数据（位于 `数据预处理结果/`），不得对原始数据文件或已有的方案、脚本、报告执行删除操作。如需覆盖或更新文件，应先确认用户意图。
- 必须严格按照给定方案执行，不得自行编造步骤、指标、结论或结果。
- 不得跳过方案中明确要求的步骤；如果实现条件不足，应先报告缺失项并等待指示，而不是自行替换或省略。
- 遇到方案冲突、歧义、缺失信息或无法自行定夺的问题时，必须立即向用户提问并等待回复后再继续。
- 只有在用户明确授权或方案本身允许时，才可以偏离原方案；否则以方案为准。
- 默认强制多线程执行，充分利用 8 核 CPU 性能；在 Python 分析脚本中应默认设置并行度为 8（或在其他环境中等价地吃满 8 核），除非方案明确指定其他并行策略或任务本身不适合并行。

## 工作方式

- 涉及题目要求、数据口径、特征工程或模型结论时，先查阅相关题目内容和数据结构；与这些内容无关的操作不必重复阅读原题。不要根据字段名臆测医学含义或直接删除异常观测。
- 优先选择可解释、适合小样本的基线模型，再与更复杂模型比较；报告模型选择依据，而不是只展示最优分数。
- 遇到会影响实现方向、数据解释或交付方式的不明确之处，应尽早直接提问，不要在关键假设不清时自行展开大量工作。
- 用户明确要求推送/提交时，完成必要检查后直接执行，不要反复确认或进行冗长说明；只有发现阻塞、风险或需要用户决策时才暂停并说明。
- 当前设备运行问题1分析脚本时不可用 `statsmodels`，不要把它作为运行依赖。问题1脚本已使用 NumPy 实现 Benjamini-Hochberg FDR 校正，保持方案中的多重比较校正口径不变。目标设备若仍缺少其他科学计算依赖，应只报告实际缺失项，不修改原始数据。
- 环境排障记录：设备先因缺少 `seaborn` 在导入阶段失败，后因缺少 `statsmodels` 在导入阶段失败；两项依赖均已从问题1脚本中移除。后续遇到同类 `ModuleNotFoundError` 时，先确认依赖是否仅用于绘图或统计辅助，再优先使用已有 `matplotlib`、`numpy`、`scipy` 实现等价功能，避免在设备上安装额外库；替换后必须执行语法检查并搜索确认旧依赖未被引用。
- 报告与图形排版规范：中文统一使用“微软雅黑（Microsoft YaHei）”，英文、数字和统计符号统一使用“Times New Roman（新罗马）”。问题1脚本生成图形时必须调用统一字体设置，不得只设置中文字体而遗漏英文、数字或图例；分析报告、表格、图题和坐标轴也遵循同一规范。
- 重新运行与报告同步：问题1脚本重新运行后，应优先读取 `问题1/问题1结果/` 下最新 CSV 和 PNG，再更新 `问题1/问题1分析报告.md` 中的实际数值和结论；不得用旧结果覆盖新结果，也不得在未运行时虚构 AUC、组合指标或聚类数值。
- 问题1章节与方案对齐：报告必须严格按方案编号组织，3.1 按功能组逐组展开分析，第 7 节为辅视角（参考区间异常模式），第 8 节为对照框架（血象表型聚类），并在报告开头明确回答原题问题 1 的具体要求。
- 数据修正记录：预处理脚本已修正两类数据问题——(1) WBC 单位混用：流感 A 组序号 345 的 WBC=7385.0（×10⁶/L 混入 ×10⁹/L 列），已除以 1000 修正为 7.385；(2) RDW-SD 混入 RDW-CV 列：序号 456（RDW=40.2）和序号 465（RDW=39.4）的值符合 RDW-SD（fL）正常范围而非 RDW-CV（%），已剔除。修正后样本量从 530 变为 528（流感 A 组 476 + 健康组 52）。修正后必须重跑预处理脚本和问题1分析脚本，报告中的所有统计量以修正后结果为准。
- 比值放大说明：MLR/NLR/PLR 的高 AUC 部分来自 L 极小值导致的比值放大效应，报告中必须说明这一点，不能将 0.990 的 AUC 直接解读为"强判别力"。
- Bootstrap CI：关键统计量（中位数、IQR）的 95% bootstrap 置信区间已在预处理阶段输出，问题1报告正文应引用这些 CI，特别是健康组 52 例的区间较宽时。
