# AGENTS.md — 2025年高教社杯全国大学生数学建模竞赛 C题

## 项目概述

**题目**：C题 微网与外部电网电力调控策略
**竞赛**：2025年全国大学生数学建模竞赛（CUMCM）
**领域**：微电网优化调度 / 线性规划 / 混合整数规划 / 随机优化

### 求解器选择

- LP：`scipy.optimize.linprog(method='highs')`
- MILP：`scipy.optimize.milp`（底层 HiGHS 分支切割法）

### 结果写入规范

- 所有 result\*.xlsx 必须**按附件5模板原结构写入**，不新建工作簿
- 模板行标签（时间段、时刻等）保持不动，只填数值
- 每个问题独立脚本，输出到 `Q<N>/result<N>.xlsx`

## 性能优化规范

### 多线程/多进程并行计算

- **原则**：所有能用多线程/多进程的并行化计算尽量使用，尽可能占满 CPU 性能，缩短解题时间，**禁止影响精度**
- **适用场景**：
  - 逐日滚动求解：各天的预测、场景生成相互独立，可用 `concurrent.futures.ProcessPoolExecutor` 并行处理多个日期
  - 场景内计算：单个日期的多场景 MILP 子问题可并行求解
  - 后处理：统计汇总、结果输出可并行
- **精度保证**：并行只是改变计算顺序和方式，不得改变数学模型、约束方程、求解器参数或数值算法，确保结果与串行完全一致
- **注意事项**：
  - 每个日期的历史数据依赖关系必须保持（不能并行处理有前后依赖的日期）
  - 可使用进程池而非线程池（CPU 密集型 MILP 求解更适合多进程）
  - 设置合理的并发数（CPU 为 8 核，固定使用 8 个进程）

## Agent 工作流程

1. **读题**：先读 `C题.pdf` 和对应问题的建模思路文档（如 `Q1/问题一_日前购电策略建模思路.md`），每次对话都要读，全文！！！强制！！！
2. **读数据**：确认附件数据格式、口径、缺失情况
3. **建模**：严格按思路文档的数学模型实现，不随意增删约束
4. **求解**：先 MILP 基准验证，再 LP 两阶段优化，对比成本一致性；**逐日滚动时启用并行求解**
5. **验证**：检查所有物理约束（功率平衡、光伏平衡、SOC边界、初末SOC、充放电互斥）
6. **输出**：按模板填写 Excel，打印汇总表到终端
7. **复核**：对照建模思路文档逐项核验代码实现

## 注意事项

- 不要修改附件文件，只读取
- 每次对话之前要先读题，搞清楚意图和接下来要做什么再行动
- 先制修订计划，然后再行动
- 结果文件写到 `Q<N>/result<N>.xlsx`，不覆盖附件5的模板
- 任何参数调整（如 P_buy_max）必须有文档依据，不得自行假设
- 发现问题1的infeasible报错时，优先检查约束方程符号是否正确，而非放宽约束
- 所有数值结果保留4位小数，与模板格式一致
- 必须勤对用户发问，任何不确定的东西涉及决策的地方都要征得用户的意见！！
- **尽可能多地向用户提问**：凡是涉及决策、参数选择、实现方案、边界条件的地方，都要逐一确认，不要自行假设。宁可多问，不要少问。
- **禁止在本机运行代码**：本机（aarch64 Linux）环境不完整，用户将在 Windows 环境下运行，所有脚本只需编写正确，无需执行测试。
- **并行求解**：问题二等逐日滚动问题，日期之间无依赖关系时，必须使用多进程并行求解，并发数固定为 8；问题一、三、四中的场景计算也可并行化。
