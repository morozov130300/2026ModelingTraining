# 流感 A 辅助筛查研究 —— 数据预处理方案（问题 1 第一阶段）

> 适用范围：两份数据文件（流感 A 患者组 + 健康对照组）的整理、清洗、合并与预处理。
> 目标：产出一份"可复现、可回溯、信息损失最小"的干净数据，供问题 1 后半段（分布差异分析、变量筛选）与问题 2（建模）使用。
> 模型定位声明：本研究的模型结果仅用于数学建模意义上的辅助筛查研究，不作为临床诊断依据。

---

## 0. 总体流程概览

```
步骤1 数据盘点与格式统一
   ↓
步骤2 合并与标签构建
   ↓
步骤3 缺失值诊断与处理
   ↓
步骤4 异常值与极端值处理（保留优先）
   ↓
步骤5 分布检查与变换决策（log / Box-Cox）
   ↓
步骤6 变量分类与判别潜力初筛（不建模）
   ↓
步骤7 样本量差异核查与影响说明
   ↓
步骤8 输出物归档（报告素材）
```

全程原则：**每一步操作可回溯（清洗日志），每个决策写明理由与对后续建模的影响。**

---

## 1. 步骤 1：数据盘点与格式统一

**目的**：把两份文件变成"列名一致、单位一致、类型一致"的表。

| 子步骤 | 具体操作 | 产出 |
|---|---|---|
| 1.1 结构盘点 | 分别读入两份文件，打印列名、行数、列类型、缺失标记样式 | 结构对照表 |
| 1.2 列名映射 | 建立两份文件列名的对应关系表（如 A 文件 `WBC` = B 文件 `白细胞计数`） | 列名映射表（入附录） |
| 1.3 单位统一 | 逐列核查单位。已知血液指标为 WBC/N/L/M（×10⁹/L）、RBC（×10¹²/L）、HB（g/L）、PLT（×10⁹/L）、RDW（%）；若出现其他单位（如 mg/dL vs mmol/L）需换算并验证量级 | 单位清单 |
| 1.4 类型清洗 | 文本型数字（`">200"`、`"—"`、`5.6x10^9/L`）转数值；统一缺失标记（空值/`NA`/`-`/`999` → NaN） | 类型统一的表 |
| 1.5 分类变量规范化 | 性别等统一编码（男/女/M/F/1/2 → 0/1），记录编码规则 | 编码规则表 |
| 1.6 重复检查 | 若有患者 ID，查重；确认同一患者不会同时出现在两类样本中 | 去重后行数 |

**对后续建模的影响**：单位不统一会使模型系数失去意义；类型错乱导致建模读入错误数值；重复样本造成训练/测试泄漏，使验证结果虚高。

---

## 2. 步骤 2：合并与标签构建

- 纵向拼接两份数据，新增标签列 `label`：流感 A 组 = 1，健康组 = 0
- 合并后确认总样本量 N、两组样本量 n₁ / n₂，并记录
- 输出：`data_merged.csv`（整理后总表，含标签列）

---

## 3. 步骤 3：缺失值诊断与处理

**原则：先诊断，再处理；"没做检查"本身可能携带信息。**

| 子步骤 | 具体操作 |
|---|---|
| 3.1 缺失率统计 | 逐变量、**按组分别**统计缺失率（流感组 vs 健康组分开报） |
| 3.2 缺失模式分析 | 缺失相关矩阵 / 缺失热图，判断缺失是否集中在特定变量或特定样本 |
| 3.3 处理策略 | ① 描述性分析阶段：中位数插补或整行剔除（仅用于画分布图）<br>② 建模阶段：保留缺失指示变量 + （可选）多重插补<br>③ 分类变量缺失：保留为"未知"类 |
| 3.4 变量处置标记 | 缺失率 > 40% 的变量标注为"建模时慎用/剔除候选"，写明理由 |

**对后续建模的影响**：医学数据缺失往往非随机（某些检验可能因患者状态未做），删除含缺失样本会让小样本进一步缩水；均值填充会抹掉缺失模式。保留缺失指示变量是最稳妥的做法。

---

## 4. 步骤 4：异常值与极端值处理（保留优先策略）

**原则（明确声明）：尽可能保留异常值以免漏掉信息；仅剔除可判定的录入错误。**

### 4.1 三层判断流程

| 层次 | 判定标准 | 处理 |
|---|---|---|
| 第一层：录入错误 | 负值（对不可能为负的指标）、超出物理测量范围的值（如 HB > 200 g/L） | **剔除**，记入清洗日志 |
| 第二层：统计可疑值 | 稳健 z-score：`(x − 中位数) / (1.4826 × MAD)`，\|z\| > 3~4；或箱线图 1.5×IQR 界限 | **标记，不删除** |
| 第三层：极端但合理 | 落在第二层标记内但无录入错误证据 | **保留**，靠步骤 5 的变换收敛其影响 |

### 4.2 处理动作汇总

- 剔除的仅限第一层（录入错误），数量与理由全部记录
- 第二、三层全部保留；可选做 winsorize（0.5%/99.5% 分位）并保留"曾截断"标记列
- **按组分别检查**：流感组的极端值可能多于健康组，这本身是差异线索，不要提前清掉

**对后续建模的影响**：血检指标天然偏态且存在真实极端生理值。直接删除会损失判别信息（极端值可能就是高风险信号）；不做处理会让逻辑回归系数被个别大值主导。采用"剔除错误 + 保留极端 + 变换/稳健处理"中间路线，并报告这一选择。

---

## 5. 步骤 5：分布检查与变换决策（log / Box-Cox）

**前提确认**：N/L/M 为数值型绝对计数，单位 ×10⁹/L → 按计数型右偏指标处理。

### 5.1 预判表（基于已知分布特征，最终以数据校验为准）

| 指标 | 类型 | 分布特征 | 变换建议 |
|---|---|---|---|
| WBC | 计数型 | 右偏长尾（正常 4-10，感染可至 20-30 ×10⁹/L） | **取 log** |
| N | 计数型 | 绝对值右偏最严重（感染可飙至 30+ ×10⁹/L） | **取 log** |
| L | 计数型 | 中度右偏 | **取 log** |
| M | 计数型 | 右偏 | **取 log** |
| PLT | 计数型 | 右偏长尾（可至 700-1000 ×10⁹/L） | **取 log** |
| RBC | 浓度型 | 生理调控严格，范围窄（约 4.3-5.8 ×10¹²/L），近似正态 | 不取 log |
| HB | 浓度型 | 严格调控，近似正态 | 不取 log |
| RDW | 比值型（%） | 范围极窄（约 11.5-14.5%），近似正态 | 不取 log（log 会过度扭曲） |

### 5.2 数据驱动验证流程（每个指标独立执行）

1. 计算原始值偏度（skewness）；偏度 > 1 判为明显右偏
2. 对每个指标做 **Box-Cox 变换**，估计 λ：
   - λ ≈ 0 → log 变换是该数据推荐的选择
   - λ ≈ 1 → 无需变换
3. 变换前后对比：偏度下降、直方图更对称、Shapiro-Wilk 检验 p 值上升 → 确认变换有效
4. log 前处理 0 或负值：若存在，用 `log(x + c)`（小常数）或 `log(x + 1)`
5. 产出"变换决策表"：每个指标的原始偏度、λ 估计、变换后偏度 → 作为报告素材

### 5.3 与"保留异常值"的关系说明

log 变换是压缩右偏长尾的刻度变换，**不删除任何记录**：极端值的相对地位保留，但影响力被收敛。它是"保留异常值"前提下最推荐的预处理手段。

---

## 6. 步骤 6：变量分类与判别潜力初筛（不建模）

**目的**：为问题 1 后半段的差异分析与问题 2 建模铺路。

| 子步骤 | 操作 | 产出 |
|---|---|---|
| 6.1 类型划分 | 基本信息类（年龄、性别等）与血液检测指标类（WBC/N/L/M/RBC/HB/PLT/RDW） | 变量类型表 |
| 6.2 分布形态 | 逐变量偏度、峰度、直方图/QQ 图，判定正态性 | 分布形态表 |
| 6.3 判别潜力初筛 | 单变量 Mann-Whitney U 检验 + 效应量（Cliff's delta），BH-FDR 多重比较校正，按 p 值与效应量分"高/中/低"三档。**仅排序，不据此删变量** | 潜力分级表 |
| 6.4 共线性初查 | 相关矩阵热图 + VIF，标出明显冗余指标组（如 WBC 与 N） | 相关热图 + VIF 表 |

---

## 7. 步骤 7：样本量差异核查与影响说明（问题明示重点）

1. 报告 n₁（流感组）、n₂（健康组）及比值；差异悬殊（如 > 1:3）时全文对比都注明背景
2. 影响说明（写进报告）：
   - 描述性对比：小样本侧中位数/IQR 不稳定，需 bootstrap 置信区间
   - 检验功效：样本少的一侧决定可发现的最小差异；检验不显著 ≠ 无差异
   - 建模影响：EPV 法则按少数类计数，直接约束可进入模型的变量数上限
3. 应对原则：**不因平衡而重采样**（小样本下 SMOTE/欠采样会扭曲数据）；如实报告，列为模型局限
4. 人口学（年龄/性别）两组是否平衡一并量化，决定差异分析是否需要分层

---

## 8. 步骤 8：输出物归档

| 输出物 | 内容 |
|---|---|
| `data_merged.csv` | 整理后合并表（含标签列），可直接用于建模 |
| 数据字典 | 每变量：原始名/统一名/单位/类型/缺失率/处理动作/变换方式 |
| 清洗日志 | 每步操作：删除行数、修改内容、理由 |
| 变量体检表 | min/max/偏度/极端值数量/可疑值清单 |
| 变换决策表 | 每指标：原始偏度、Box-Cox λ、变换后偏度 |
| 潜力分级表 | 单变量检验 p 值（校正后）、效应量、潜力分档 |
| 附图 | 缺失热图、分布直方图（变换前后对比）、相关热图、箱线图/小提琴图 |

---

## 9. 对后续建模影响的汇总

| 预处理决策 | 对后续建模的影响 |
|---|---|
| 单位/格式统一 | 系数与概率输出有可比意义 |
| 缺失保留 + 指示变量 | 保留"未检查"这一潜在判别信息，避免样本缩水 |
| 异常值保留 + log 变换 | 极端值信息不丢，同时避免个别值主导逻辑回归系数 |
| 不重采样 | 避免人为扭曲分布；以 EPV 约束特征数为补偿 |
| 按组检查极端值 | 发现两组差异线索，供问题 1 后半段使用 |

---

## 10. 文献支撑

- Omuse et al. (2018), *Complete blood count reference intervals from a healthy adult urban population*, PLOS One. https://journals.plos.org/plosone/article?id=10.1371/journal.pone.0198444
- CLSI C28-A3, *Defining, Establishing, and Verifying Reference Intervals in the Clinical Laboratory*
- Box & Cox (1964), *An Analysis of Transformations*, JRSS-B
- Templeton et al. (2015), *The neutrophil–lymphocyte ratio and its utilisation for the prediction of outcome in patients with cancer*, Br J Cancer. https://www.nature.com/articles/bjc201567
- Luo et al., *Optimal cut-off for neutrophil-to-lymphocyte ratio: Fact or Fantasy?*, PLOS One. https://journals.plos.org/plosone/article?id=10.1371/journal.pone.0195042
- StatPearls, *Normal and Abnormal Complete Blood Count With Differential*. https://www.ncbi.nlm.nih.gov/books/NBK604207/
