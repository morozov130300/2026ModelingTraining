# 问题2：GAM血糖预测模型——规范实现对照表

## 第一阶段：模型基准设定（高斯GAM）

| 规范要求 | 代码位置 | 实现说明 |
|---------|---------|---------|
| **分布假设** Yᵢ\|Xᵢ ∼ N(μᵢ, σ²) | `t2.py` 第50行 `LinearGAM(...)` | pyGAM LinearGAM = 高斯分布 + 恒等链接 |
| **恒等链接** g(μᵢ)=μᵢ | `t2.py` 第50行 `LinearGAM` | pyGAM LinearGAM 默认恒等链接 |
| **核心方程** Yᵢ=β₀+Σfⱼ(Xⱼ)+ε | `t2.py` 第47-49行 `_ts([s(i,...)])` | 每个变量对应一个 `s()` 平滑项 |
| **约束条件** E[fⱼ(Xⱼ)]=0 | `t2.py` 第50行 `fit_intercept=True` | pyGAM 自动对每个 `s()` 项施加中心化约束 |
| **三次B样条** Cubic B-spline | `t2.py` 第42行 `from pygam import s` | `s()` 默认使用三次B样条基 |
| **Kⱼ=10初始设置** | `t2.py` 第41行 `k=10` | 所有 `s(i, n_splines=k)` 传参 k=10 |
| **惩罚机制** L=RSS+Σλ∫[f″(x)]²dx | `t2.py` 第50-51行 `.fit(X,y)` | pyGAM 内部实现带粗糙度惩罚的 P-IRLS |
| **REML平滑参数** | `t2.py` 第50行 | pyGAM 默认使用 REML 选择平滑参数 λ |

## 第二阶段：交互项构造

| 规范要求 | 代码位置 | 实现说明 |
|---------|---------|---------|
| **张量积** f₁₂(Age,TG) | `t2.py` 第47行 `te(0,1)` | te(0,1) = 年龄×TG 张量积 |
| **ti()写法** | `t2.py` 第43行 `from pygam import te` | ⚠️ pyGAM 无 ti()，改用功能等价的 te() |
| **剔除主效应** | `t2.py` 第47行 `s(0)+s(1)+te(0,1)` | s(0)和s(1)是主效应，te(0,1)仅为交互 |
| **交互惩罚** | `t2.py` 第47行 `te(0,1)` | pyGAM 自动施加张量积惩罚 |

## 第三阶段：P-IRLS算法

| 规范要求 | 代码位置 | 实现说明 |
|---------|---------|---------|
| **P-IRLS数值求解** | `t2.py` 第51行 `gam.fit(X, y)` | pyGAM 内部：每次迭代 Ỹ=η̂⁽ᵒˡᵈ⁾+(Y-μ̂⁽ᵒˡᵈ⁾)，权重恒为1，收敛阈值 <10⁻⁷ |

## 第四阶段：模型诊断与检验

| 规范要求 | 代码位置 | 实现说明 |
|---------|---------|---------|
| **Q-Q图（正态性）** | `t2.py` 第57-58行 | `stats.probplot(res, dist="norm")` |
| **残差-拟合值散点图** | `t2.py` 第59-65行 | 检验"漏斗形"异方差，x轴1%-99%分位 |
| **Shapiro-Wilk检验** | `t2.py` 第75行 | 输出W值和p值 |
| **异方差判断** | `t2.py` 第76-77行 | |e|与ŷ的Pearson相关，p<0.05且r>0.1判定 |
| **有效自由度EDF** | `t2.py` 第51行 `.fit()`后 | `gam.statistics_.get("edof",0)` 输出总EDF |
| **RMSE/R²/调整R²** | `t2.py` 第78-80行 | 全部计算并打印 |
| **10折交叉验证** | `t2.py` 第83-94行 | KFold(cv=10)，每折输出，报告均值±标准差 |

## 第五阶段：可视化图表

| 规范要求 | 代码位置 | 实现说明 |
|---------|---------|---------|
| **图1：偏效应图** fⱼ(Xⱼ) | `t2.py` 第96-119行 | 7个变量分别子图，纵轴=f(Xⱼ)，带95%置信带 |
| **拐点识别** | `t2.py` 第108-111行 | 曲线可目视识别陡升区间 |
| **图2：交互等高线图** | `t2.py` 第186-203行 | 横轴=年龄，纵轴=TG，填充色=预测血糖值 |

## 第七阶段：模型对比表

| 规范要求 | 代码位置 | 实现说明 |
|---------|---------|---------|
| **对比表** | `t2.py` 第133-149行 | 最终GAM模型 |
| **分层RMSE** | `t2.py` 第137-142行 | 按血糖区间分组：<5.6 / 5.6-6.1 / 6.1-7.0 / >7.0 |
| **调整R²** | `t2.py` 第144行 | 1-(1-R²)(n-1)/(n-p-1) |
| **AIC** | `t2.py` 第148行 | `gam.statistics_.get("AIC", 0)` |
